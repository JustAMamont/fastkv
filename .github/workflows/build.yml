name: Build & Test

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # ──────────────────────────────────────────────
  # Server build
  # ──────────────────────────────────────────────
  server:
    name: Server (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.85"

      - uses: Swatinem/rust-cache@v2

      - name: Build (default)
        run: cargo build --release --locked

      - name: Build (io_uring, Linux only)
        if: runner.os == 'Linux'
        run: cargo build --release --locked --features io-uring

      - name: Run unit tests
        run: cargo test --locked

  # ──────────────────────────────────────────────
  # Rust client
  # ──────────────────────────────────────────────
  client-rust:
    name: Rust client (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: server
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.85"

      - uses: Swatinem/rust-cache@v2

      - name: Build
        working-directory: clients/rust
        run: cargo build --release --locked

      - name: Start server
        run: cargo run --release -- server 6379 &
        shell: bash

      - name: Run integration tests
        env:
          FASTKV_HOST: localhost
          FASTKV_PORT: "6379"
        working-directory: clients/rust
        run: cargo test --locked -- --test-threads=1
        shell: bash

  # ──────────────────────────────────────────────
  # Go client — cross-compile all targets
  # ──────────────────────────────────────────────
  client-go:
    name: Go client
    runs-on: ubuntu-latest
    needs: server
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.21"

      - name: Cross-compile
        working-directory: clients/go/fastkv
        env:
          CGO_ENABLED: "0"
        run: |
          targets=(
            "linux   amd64"
            "linux   arm64"
            "windows amd64 .exe"
            "windows arm64 .exe"
            "darwin  amd64"
            "darwin  arm64"
          )
          mkdir -p bin
          for t in "${targets[@]}"; do
            read os arch ext <<< "$t"
            out="bin/fastkv-${os}-${arch}${ext}"
            echo "Building $out ..."
            GOOS=$os GOARCH=$arch go build -trimpath -ldflags="-s -w" -o "$out" .
          done

      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: go-client-binaries
          path: clients/go/fastkv/bin/

      - name: Start server
        run: cargo run --release -- server 6379 &
        shell: bash

      - name: Run tests
        env:
          FASTKV_ADDR: "localhost:6379"
        working-directory: clients/go/fastkv
        run: go test -v -count=1 -timeout 30s ./...

  # ──────────────────────────────────────────────
  # Java client — compile & test on each OS
  # ──────────────────────────────────────────────
  client-java:
    name: Java client (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: server
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        include:
          - os: macos-latest
            arch: arm64
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "8"

      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.85"

      - uses: Swatinem/rust-cache@v2

      - name: Compile
        run: |
          mkdir -p clients/java/target/classes
          javac -d clients/java/target/classes clients/java/src/*.java
        shell: bash

      - name: Start server
        run: cargo run --release -- server 6379 &
        shell: bash

      - name: Run sync tests
        env:
          FASTKV_HOST: localhost
          FASTKV_PORT: "6379"
        run: java -Dfastkv.host=localhost -Dfastkv.port=6379 -cp clients/java/target/classes com.fastkv.client.IntegrationTest
        shell: bash

      - name: Run reactive tests
        env:
          FASTKV_HOST: localhost
          FASTKV_PORT: "6379"
        run: java -Dfastkv.host=localhost -Dfastkv.port=6379 -cp clients/java/target/classes com.fastkv.client.IntegrationTestReactive
        shell: bash

  # ──────────────────────────────────────────────
  # Node.js client — test on each OS
  # ──────────────────────────────────────────────
  client-node:
    name: Node.js client (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: server
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.85"

      - uses: Swatinem/rust-cache@v2

      - name: Start server
        run: cargo run --release -- server 6379 &
        shell: bash

      - name: Run tests
        run: node clients/node/fastkv/tests/test_integration.js localhost 6379
        shell: bash

  # ──────────────────────────────────────────────
  # Python client — test on each OS
  # ──────────────────────────────────────────────
  client-python:
    name: Python client (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: server
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.9", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.85"

      - uses: Swatinem/rust-cache@v2

      - name: Start server
        run: cargo run --release -- server 6379 &
        shell: bash

      - name: Run sync tests
        env:
          FASTKV_HOST: localhost
          FASTKV_PORT: "6379"
        run: python clients/python/tests/test_integration.py
        shell: bash

      - name: Run async tests
        env:
          FASTKV_HOST: localhost
          FASTKV_PORT: "6379"
        run: python clients/python/tests/test_async_integration.py
        shell: bash
