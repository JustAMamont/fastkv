[package]
name = "fastkv-client"
version = "1.5.1"
edition = "2021"
description = "Async FastKV client for Rust (tokio)"

[dependencies]
tokio = { version = "1", features = ["net", "io-util", "macros", "rt-multi-thread"] }
